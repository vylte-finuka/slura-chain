{
  "name": "maratinekit-tool-builder",
  "version": "0.1.0",
  "private": true,
  "description": "Plugin Figma : genere le squelette source (.mara) d'une app/bureau Maratine (Slura OS) a partir d'un frame Figma selectionne.",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
